FROM node:20-slim
WORKDIR /app

# Build the server
COPY package*.json ./server/
WORKDIR /app/server
RUN npm install
COPY tsconfig.json ./
COPY src ./src
RUN npx tsc

# Set up client workspace
WORKDIR /app/client
